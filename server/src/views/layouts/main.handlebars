<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Crimson+Pro:wght@400;600;700&family=Lora:ital,wght@0,400;0,600;1,400&display=swap');
    
    :root {
      --bg-forest: #1a2f23;
      --bg-card: #2d4a3e;
      --text-light: #e8f0eb;
      --text-muted: #9fb8a9;
      --accent-gold: #d4a84b;
      --accent-amber: #d97706;
      --accent-blue: #3b82f6;
      --accent-green: #10b981;
      --accent-gray: #6b7280;
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Lora', serif;
      background: var(--bg-forest);
      color: var(--text-light);
      min-height: 100vh;
      background-image: 
        radial-gradient(ellipse at top left, rgba(16, 185, 129, 0.1) 0%, transparent 50%),
        radial-gradient(ellipse at bottom right, rgba(212, 168, 75, 0.1) 0%, transparent 50%);
    }
    
    header {
      background: linear-gradient(135deg, #0d1f17 0%, #1a2f23 100%);
      border-bottom: 2px solid var(--accent-gold);
      padding: 1.5rem 2rem;
    }
    
    header h1 {
      font-family: 'Crimson Pro', serif;
      font-size: 2.5rem;
      color: var(--accent-gold);
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }
    
    header nav {
      margin-top: 0.5rem;
    }
    
    header nav a {
      color: var(--text-muted);
      text-decoration: none;
      margin-right: 1.5rem;
      transition: color 0.2s;
    }
    
    header nav a:hover {
      color: var(--accent-gold);
    }
    
    main {
      max-width: 1200px;
      margin: 0 auto;
      padding: 2rem;
    }
    
    h2 {
      font-family: 'Crimson Pro', serif;
      font-size: 2rem;
      color: var(--accent-gold);
      margin-bottom: 1rem;
      border-bottom: 1px solid rgba(212, 168, 75, 0.3);
      padding-bottom: 0.5rem;
    }
    
    h3 {
      font-family: 'Crimson Pro', serif;
      font-size: 1.5rem;
      color: var(--text-light);
      margin: 1.5rem 0 0.75rem;
    }
    
    p {
      line-height: 1.8;
      margin-bottom: 1rem;
      color: var(--text-muted);
    }
    
    .card {
      background: var(--bg-card);
      border-radius: 12px;
      padding: 1.5rem;
      margin-bottom: 1.5rem;
      border: 1px solid rgba(212, 168, 75, 0.2);
      box-shadow: 0 4px 20px rgba(0,0,0,0.2);
    }
    
    .faction-card {
      display: flex;
      gap: 1.5rem;
      align-items: flex-start;
    }
    
    .faction-icon {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.5rem;
      flex-shrink: 0;
    }
    
    .faction-icon.ironwood { background: var(--accent-amber); }
    .faction-icon.eyrie { background: var(--accent-blue); }
    .faction-icon.alliance { background: var(--accent-green); }
    .faction-icon.wanderer { background: var(--accent-gray); }
    
    .faction-info h3 {
      margin-top: 0;
    }
    
    .faction-meta {
      display: flex;
      gap: 1rem;
      margin-top: 0.5rem;
    }
    
    .badge {
      padding: 0.25rem 0.75rem;
      border-radius: 20px;
      font-size: 0.85rem;
      background: rgba(255,255,255,0.1);
    }
    
    ul {
      margin-left: 1.5rem;
      margin-bottom: 1rem;
    }
    
    li {
      margin-bottom: 0.5rem;
      color: var(--text-muted);
      line-height: 1.6;
    }
    
    footer {
      text-align: center;
      padding: 2rem;
      color: var(--text-muted);
      border-top: 1px solid rgba(212, 168, 75, 0.2);
      margin-top: 3rem;
    }
  </style>
</head>
<body>
  <header>
    <h1>🌲 Woodland War</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/rules">Rules</a>
      <a href="/about">About</a>
    </nav>
  </header>
  
  <main>
    {{{body}}}
  </main>
  
  <footer>
    <p>Woodland War - A strategic board game for 2-4 players</p>
  </footer>
</body>
</html>
